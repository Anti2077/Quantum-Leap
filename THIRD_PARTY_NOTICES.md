# Third-Party Notices

The Windows and Linux packages include `iperf3` 3.21, built from the official
ESnet source release. `iperf3` is distributed under the three-clause BSD
license and includes components under compatible permissive licenses. The full
upstream `LICENSE` file is included in each package under `third-party/`.

The Windows package builds `iperf3` with Cygwin and includes the Cygwin runtime
libraries reported by `cygcheck`. The applicable Cygwin runtime license is also
included in the package under `third-party/`.

- iperf3 source: https://github.com/esnet/iperf/releases/tag/3.21
- Cygwin licensing: https://cygwin.com/licensing.html
